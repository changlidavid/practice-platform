# Count Of Given Digit In Numbers In List

You can assume that L is a list of positive integers and d is a digit.

## Function Signature

`count_of_given_digit_in_numbers_in_list(...)`

## Notes

- `public_examples.json` is display-only.
- `hidden_tests.json` is the official evaluation set.
- After auto-conversion, review and add extra hidden edge cases where appropriate.
