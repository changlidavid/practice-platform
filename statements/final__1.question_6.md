# Final 1.Question 6

## Problem

You might find the zip() function useful, though you can also do without it.

## Function Signature

- `display(square, size)`
- `f(for_seed, n, upper_bound)`

## Notes

- Write your code in the solution file on the right.

- The runner executes `python -m doctest -v` against your solution.
